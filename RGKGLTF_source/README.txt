RGKGLTF используется для задачи Web. Либа отвечает за интерпретацию пользовательского кода и конвертацию полученных тел в GLTF.

Для сборки библиотеки надо вызывать "python setup.py build_ext". Далее забрать либу из builds. 